version: '2'
services:
  vaktsamplebackend-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/VaktSampleBackend/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=VaktSampleBackend
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
